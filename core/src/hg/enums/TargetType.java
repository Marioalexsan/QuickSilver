package hg.enums;

/** TargetType holds target type IDs.
 * Usually NetInstructions can specify a target, and the parser can decide to act on certain classes based on that. */
public class TargetType {
    public static final int Actors = 0;
    public static final int Gamemodes = 1;
}
